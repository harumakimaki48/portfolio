<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>タグ編集画面</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <%= stylesheet_link_tag 'admin_tags', media: 'all' %>
</head>
<body>
  <h2 class="text-center">タグ編集画面</h2>
  <div class="top-button-container">
    <%= link_to 'TOP', admin_dashboard_index_path, class: 'btn btn-info float-right' %>
    </div>


  <div class="container">
<div class="tag-form">
  <%= form_with(model: @tag, url: admin_tags_path, local: true) do |form| %>
    <div class="form-group input-group">
      <% form.label :name, "新規作成" %>
      <%= form.text_field :name, class: "form-control", placeholder: "新規作成" %>
      <%= form.submit "保存", class: "btn btn-primary" %>
    </div>
  <% end %>
</div>

    <div class="tag-list">
      <% @tags.each do |tag| %>
        <div class="tag-item">
          <%= form_with(model: tag, url: admin_tag_path(tag), method: :patch, local: true) do |form| %>
            <div class="form-group input-group">
              <%= form.text_field :name, class: "form-control" %>
              <%= form.submit "保存", class: "btn btn-primary mx-2" %>
            </div>
          <% end %>
          <div style="text-align: right;">
          <%= button_to "削除", admin_tag_path(tag), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "btn btn-danger mx-2" %>
          </div>
          </div>
      <% end %>
    </div>

  </div>
</body>
</html>
