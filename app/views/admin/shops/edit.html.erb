<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>店舗情報編集</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
  <h1 class="text-center">梅ヶ枝餅屋さんリスト</h1>
  <div class="container mt-4">
    <%= form_with model: @shop, url: admin_shop_path(@shop), local: true do |f| %>
      <div class="form-group">
        <%= f.label :name, "店舗名" %>
        <%= f.text_field :name, class: "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :url, "URL" %>
        <%= f.text_field :url, class: "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :opening_time, "営業時間" %>
        <%= f.time_select :opening_time, class: "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :closing_time, "閉店時間" %>
        <%= f.time_select :closing_time, class: "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :holiday, "定休日" %>
        <%= f.text_field :holiday, class: "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :tags, "タグ" %>
        <%= f.collection_select :tag_ids, Tag.all, :id, :name, { include_blank: false }, { multiple: true, class: "form-control" } %>
      </div>
      <%= f.submit "保存", class: "btn btn-primary" %>
    <% end %>
  </div>
</body>
</html>