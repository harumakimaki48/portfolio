<!DOCTYPE html>
<html>

<html lang="ja">
<head>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>

<body>
<h1 class="text-center">梅ヶ枝餅屋さんリスト</h1>

<div class="container">
  <div class="row mb-3">
    <%= search_form_for @q do |f| %>
      <div class="col-md-15 d-flex align-items-center">
        <%= f.text_field :name_cont, class: "form-control", placeholder: "あいまい検索" %>
        <%= f.submit "検索", class: "btn btn-primary ml-2" %>
      </div>
    <% end %>
  </div>
  
  <div class="row">
    <% @shops.each do |shop| %>
      <div class="col-md-6 mb-3">
        <div class="card">
          <div class="card-body">
            <h3 class="card-title"><%= shop.name %></h3>
            <p class="card-text">
              <strong>営業時間:</strong> <%= shop.opening_time.strftime('%H:%M') %>〜<%= shop.closing_time.strftime('%H:%M') %><br>
              <strong>定休日:</strong> <%= shop.holiday %><br>
              <strong>タグ:</strong> <---タグを表示予定---><br>
              <strong>URL:</strong><%= link_to shop.url, shop.url, target: "_blank", class: "btn btn-link" %>
            </p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
</body>

</html>